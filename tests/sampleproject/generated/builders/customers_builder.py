#!/usr/bin/env python
# -*- coding: utf-8 -*-
"""
SQL Query Builder for customers table

Auto-generated by pyStoOrm - Do not edit manually!
Provides SQL fragments and helpers for building custom queries.
"""
from typing import List, Optional, Tuple, Any
import sys
import os

# Import model and repository from sibling directories
_models_dir = os.path.join(os.path.dirname(__file__), '..', 'models')
_repos_dir = os.path.join(os.path.dirname(__file__), '..', 'repositories')
sys.path.insert(0, _models_dir)
sys.path.insert(0, _repos_dir)

from customers import Customers
from customers_repository import CustomersRepository




class CustomersBuilder:
    """
    SQL Query Builder for customers table.

    Provides methods to build SQL fragments and convert database rows to model objects.
    Useful for building custom queries with JOINs and complex WHERE clauses.

    Example:
        # Custom JOIN query
        cursor.execute(f"
            SELECT {CustomersBuilder.select('t')}, {OrderBuilder.select('o')}
            FROM {CustomersRepository.SQL_TABLE} t
            JOIN orders o ON o.customers_id = t.id
            WHERE t.status = ?
        ", ['active'])

        for row in cursor:
            customers = CustomersBuilder.from_row(row[:13])
            order = OrderBuilder.from_row(row[13:])
    """

    # ============================================================
    # SQL Fragment Generators
    # ============================================================

    @staticmethod
    def select(alias: str = None) -> str:
        """
        Generate SELECT clause with table alias.

        Args:
            alias: Table alias (e.g., 't', 'c', 'p'). Defaults to SQL_ALIAS.

        Returns:
            SQL fragment: "alias.col1, alias.col2, ..."

        Example:
            SELECT {CustomersBuilder.select('t')}, ... FROM ...
        """
        alias = alias or CustomersRepository.SQL_ALIAS
        cols = ', '.join([f'{alias}.{col}' for col in CustomersRepository.SQL_COLS])
        return cols

    @staticmethod
    def select_with_table(alias: str = None) -> str:
        """
        Generate SELECT clause with table alias (alternative name).

        Same as select() - for readability.
        """
        return CustomersBuilder.select(alias)

    @staticmethod
    def where_pk(alias: str = None) -> str:
        """
        Generate WHERE clause for primary key.

        Args:
            alias: Table alias (optional)

        Returns:
            WHERE fragment: "alias.pk_col = ?" or "pk_col = ?"

        Example:
            WHERE {CustomersBuilder.where_pk('t')} AND ...
        """
        alias = alias or CustomersRepository.SQL_ALIAS
        pk = CustomersRepository.SQL_PK
        return f'{alias}.{pk} = ?' if alias else f'{pk} = ?'

    @staticmethod
    def table() -> str:
        """Get table name."""
        return CustomersRepository.SQL_TABLE

    @staticmethod
    def alias() -> str:
        """Get default table alias."""
        return CustomersRepository.SQL_ALIAS

    @staticmethod
    def pk() -> str:
        """Get primary key column name."""
        return CustomersRepository.SQL_PK

    @staticmethod
    def columns() -> List[str]:
        """Get list of column names."""
        return CustomersRepository.SQL_COLS

    @staticmethod
    def column_index(col_name: str) -> int:
        """
        Get array index for a column.

        Args:
            col_name: Column name

        Returns:
            Array index (0-based)

        Raises:
            KeyError: If column not found
        """
        return CustomersRepository.SQL_IDX[col_name]

    @staticmethod
    def column_count() -> int:
        """Get number of columns in table."""
        return len(CustomersRepository.SQL_COLS)

    # ============================================================
    # Row to Model Conversion
    # ============================================================

    @staticmethod
    def from_row(row: Tuple[Any, ...], start_idx: int = 0, end_idx: int = None) -> Customers:
        """
        Create model instance from a portion of database row.

        Useful for extracting one table's data from a JOIN result.

        Args:
            row: Database row (tuple)
            start_idx: Starting index in row (default: 0)
            end_idx: Ending index (exclusive). If None, uses column count.

        Returns:
            Customers model instance

        Example:
            # From single table query
            row = cursor.fetchone()
            model = CustomersBuilder.from_row(row)

            # From JOIN query (extract first table)
            row = cursor.fetchone()
            count = CustomersBuilder.column_count()
            model = CustomersBuilder.from_row(row, 0, count)
        """
        if end_idx is None:
            end_idx = start_idx + CustomersBuilder.column_count()

        row_slice = row[start_idx:end_idx]
        column_order = CustomersRepository.SQL_COLS

        return Customers._from_row(row_slice, column_order)

    @staticmethod
    def from_rows(rows: List[Tuple[Any, ...]], start_idx: int = 0, end_idx: int = None) -> List[Customers]:
        """
        Create multiple model instances from rows.

        Args:
            rows: List of database rows
            start_idx: Starting column index (default: 0)
            end_idx: Ending column index (exclusive). If None, uses column count.

        Returns:
            List of Customers model instances

        Example:
            rows = cursor.fetchall()
            models = CustomersBuilder.from_rows(rows)
        """
        return [CustomersBuilder.from_row(row, start_idx, end_idx) for row in rows]

    @staticmethod
    def batch_from_rows(rows: List[Tuple[Any, ...]],
                       key_func=None) -> dict:
        """
        Create model instances from rows and batch by primary key.

        Useful for de-duplicating results from self-joins or similar queries.

        Args:
            rows: List of database rows
            key_func: Optional function to extract grouping key from row.
                     Defaults to extracting primary key.

        Returns:
            Dictionary: {primary_key_value: model_instance}

        Example:
            rows = cursor.fetchall()
            models_by_id = CustomersBuilder.batch_from_rows(rows)
        """
        if key_func is None:
            pk_idx = CustomersBuilder.column_index(CustomersRepository.SQL_PK)
            key_func = lambda row: row[pk_idx]

        result = {}
        for row in rows:
            model = CustomersBuilder.from_row(row)
            key = key_func(row)
            result[key] = model

        return result

    # ============================================================
    # SQL Type Information
    # ============================================================

    @staticmethod
    def types() -> str:
        """
        Get type mapping string for prepared statements.

        Format: i=int, s=string, d=decimal/float, b=bool

        Returns:
            Type string (e.g., 'isds')

        Example:
            # For use with database libraries that require type info
            types = CustomersBuilder.types()
        """
        return CustomersRepository.SQL_TYPES

    @staticmethod
    def column_types() -> dict:
        """
        Get type mapping for each column.

        Returns:
            Dictionary: {column_name: type_char}
        """
        types_str = CustomersBuilder.types()
        columns = CustomersBuilder.columns()
        return {col: types_str[i] for i, col in enumerate(columns)}
